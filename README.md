# prompt-chat
Prompt Chat is a monorepo for a multi-platform LLM chat app that lets you define a system prompt and then chat with an LLM using that prompt. Includes an Expo client (iOS, Android, Web, mobile web) and a Node.js API, deployable via Docker.
